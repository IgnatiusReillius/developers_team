
<div class="flex">
    <?php foreach ($tasks as $task): ?>
        <div class="mt-10 bg-white rounded-2xl shadow-md p-6 border border-gray-200">
            <!-- Título de la tarea -->
            <h2 class="text-2xl font-semibold text-gray-800 mb-4">
                <?= htmlspecialchars($task->taskTitle) ?>
            </h2>
    
            <!-- Descripción -->
            <div class="border border-gray-300 rounded-lg p-4 text-gray-700 mb-4">
                <?= htmlspecialchars($task->taskDescription) ?>
            </div>
        </div>
    <?php endforeach; ?>
</div>